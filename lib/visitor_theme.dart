import 'package:flutter/material.dart';

/// Shared theme constants for the Visitor Portal (web-first design).
const Color visitorPrimary = Color(0xFF0047AB);
const Color visitorSidebar = Color(0xFF0A192F);
const Color visitorBg = Color(0xFFF8F9FB);
const Color visitorAccent = Color(0xFF1E88E5);
const Color visitorNavy = Color(0xFF001F3F);

/// Indigo used for authenticated student AI chat (per spec).
const Color chatIndigo = Color(0xFF3F51B5);
